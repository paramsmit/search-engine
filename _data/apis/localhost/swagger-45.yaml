---
swagger: "2.0"
info: !php/object "O:8:\"stdClass\":2:{s:7:\"version\";s:5:\"1.0.0\";s:5:\"title\";s:18:\"wicked
  Echo Server\";}"
host: localhost:3009
basePath: /portal-health/v1
paths:
  /*:
    get:
      description: Returns the path and headers of the requet
      responses:
        200:
          description: Returns a JSON representation of the requets
          schema:
            type: object
            properties:
              message:
                type: string
    put:
      description: Returns the path and headers of the request
      responses:
        200:
          description: Returns a JSON representation of the requets
          schema:
            type: object
            properties:
              message:
                type: string
    post:
      description: Returns the path and headers of the request
      responses:
        200:
          description: Returns a JSON representation of the requets
          schema:
            type: object
            properties:
              message:
                type: string
    patch:
      description: Returns the path and headers of the requet
      responses:
        200:
          description: Returns a JSON representation of the requets
          schema:
            type: object
            properties:
              message:
                type: string
    delete:
      description: Returns the path and headers of the requet
      responses:
        200:
          description: Returns a JSON representation of the requets
          schema:
            type: object
            properties:
              message:
                type: string
schemes:
- http
...
