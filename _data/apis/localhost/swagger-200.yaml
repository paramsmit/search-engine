---
swagger: "2.0"
info: !php/object "O:8:\"stdClass\":3:{s:11:\"description\";s:393:\"The Busapp aims
  to facilitate the consultation of information regarding the various bus lines, as
  well as their paths, values and delay averages. Aiming the greatest comfort and
  security when using public transport. The software also tries to make users save
  time knowing the exact time that the transport will reach the stop / destination,
  avoiding downtime waiting for transport at the stop.\";s:5:\"title\";s:6:\"Busapp\";s:7:\"version\";s:5:\"1.0.0\";}"
host: localhost
basePath: /api
paths:
  /Admin:
    get:
      description: Gets `Person` objects.nOptional query param of **size** determinesnsize
        of returned arrayn
      parameters:
      - name: size
        in: query
        description: Size of array
        required: true
        type: number
        format: double
      responses:
        200:
          description: Successful response
          schema:
            title: ArrayOfPersons
            type: array
            items:
              title: Person
              type: object
              properties:
                name:
                  type: string
                single:
                  type: boolean
produces:
- application/json
schemes:
- https
...
